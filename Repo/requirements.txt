# Dépendances minimales (strict minimum utilisé par les scripts)
# - Contient uniquement les paquets effectivement importés dans les scripts
# - Commentaires en français pour guider les développeurs

# Paquets essentiels (installer via `pip install -r requirements.txt`)
numpy==1.26.4
pandas
scikit-learn
tqdm

# Remarques :
# - `pandas` et `scikit-learn` peuvent provenir de builds conda (apparaissent
#   parfois comme `package @ file://...` dans `pip freeze`). Si vous utilisez
#   conda, préférez créer l'env via `environment.yml` pour une reproduction exacte.
# - N'ajoutez ici que les paquets nécessaires à l'exécution des scripts.
# - Pour enrichir ce fichier automatiquement depuis l'env en retirant
#   les entrées locales conda :
#     pip freeze | grep -v "@ file://" > requirements.txt
# Dépendances minimales pour `dev_build_models` et `prod_api_rest`

# Remarques :
# - Certaines entrées de `pip freeze` peuvent apparaître sous la forme
#   `package @ file://...` lorsque les paquets proviennent de builds conda locaux.
#   Ces lignes ne se convertissent pas proprement en `package==version` pour pip.
#   Pour une reproduction exacte de l'environnement, il est recommandé d'utiliser
#   un `environment.yml` (exemple dans `environment.yml`).
# - Les modules de la bibliothèque standard (os, sys, json, pickle, datetime, typing, ...)
#   ne sont pas listés.

# Paquets identifiés par intersection (présents dans le code et dans l'environnement) :

# Épinglé quand disponible
numpy==1.26.4

# Présents dans l'environnement mais fournis comme wheels conda (à pinner via conda si besoin)
pandas
scikit-learn
tqdm

# Instructions rapides :
# - Pour reproduire avec `pip` (approche simple) :
#     python -m venv .venv
#     source .venv/bin/activate
#     pip install -r requirements.txt
# - Pour reproduire fidèlement avec `conda` (recommandé si l'origine est conda) :
#     conda env create -f environment.yml
