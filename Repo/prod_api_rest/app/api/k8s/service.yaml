apiVersion: v1
kind: Service
metadata:
  name: assurance-pricing-api
spec:
  type: ClusterIP
  selector:
    app: assurance-pricing-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
