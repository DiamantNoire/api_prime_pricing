# =============================================================================
# requirements.txt — Dépendances complètes du projet
# =============================================================================
# Couvre tous les sous-projets :
#   - dev_build_models/   → entraînement des modèles ML
#   - prod_api_rest/app/api/       → API REST FastAPI
#   - prod_api_rest/app/src/frontend/ → interface Streamlit
#
# Installation rapide (environnement virtuel) :
#   python -m venv .venv
#   source .venv/bin/activate          # Linux/macOS
#   .venv\Scripts\activate             # Windows
#   pip install -r requirements.txt
#
# Recommandé pour la reproductibilité exacte (conda) :
#   conda env create -f environment.yml
#   conda activate env_MATHS_FINANCE_DS_ML
#
# Les modules de la bibliothèque standard (os, sys, json, pickle, typing…)
# ne sont pas listés.
# =============================================================================


# -----------------------------------------------------------------------------
# Core — partagé par tous les sous-projets
# -----------------------------------------------------------------------------
numpy==1.26.4
pandas==2.2.3


# -----------------------------------------------------------------------------
# ML — dev_build_models (entraînement fréquence + sévérité)
# -----------------------------------------------------------------------------
scikit-learn==1.5.2
tqdm>=4.66


# -----------------------------------------------------------------------------
# API REST — prod_api_rest/app/api (FastAPI)
# -----------------------------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4


# -----------------------------------------------------------------------------
# Frontend — prod_api_rest/app/src/frontend (Streamlit)
# -----------------------------------------------------------------------------
streamlit>=1.30
sqlalchemy>=2.0
plotly>=5.18
openpyxl>=3.1
toml>=0.10
requests>=2.31


# -----------------------------------------------------------------------------
# Dev / Tests
# -----------------------------------------------------------------------------
pytest>=7.4
httpx>=0.27          # client HTTP async pour tester l'API FastAPI
ruff>=0.4            # linter/formateur
